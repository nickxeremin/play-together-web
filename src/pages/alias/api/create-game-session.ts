import { useMutation } from "@tanstack/react-query"

// import { client } from "@/shared/api/client"

// import { CreateAliasGameValues } from "../model/create-game"

// export function useCreateAliasGameSession() {
//     return useMutation({
//         mutationFn: (values: CreateAliasGameValues) =>
//             client
//                 .post("/alias/sessions", {
//                     values,
//                 })
//                 .then(({ data }) => data as { session_id: string }),
//     })
// }
